---
import { getCollection } from 'astro:content';

const allDocs = await getCollection('docs');
const first = allDocs.sort((a, b) => (a.data.order ?? 999) - (b.data.order ?? 999))[0];

return Astro.redirect(`/docs/${first?.slug ?? 'overview'}`);
---
